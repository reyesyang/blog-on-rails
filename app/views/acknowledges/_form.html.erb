<%= form_for(@acknowledge) do |f| %>
  <% if @acknowledge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@acknowledge.errors.count, "error") %> prohibited this acknowledge from being saved:</h2>

      <ul>
      <% @acknowledge.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, t('acknowledge_name') %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :image_url, t('acknowledge_image_url') %><br/>
    <%= f.text_field :image_url %>
  </div>
  <div class="field">
    <%= f.label :website, t('acknowledge_website') %><br/>
    <%= f.text_field :website %>
  </div>
  <div class="field">
    <%= f.label :description, t('acknowledge_description') %><br />
    <%= f.text_area :description, :class => "markitup_setting" %>
  </div>
  <div class="actions">
    <%= f.submit t('create_acknowledge') %>
  </div>
<% end %>
